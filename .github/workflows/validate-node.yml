name: Validate Node project

on:
  workflow_call:
    inputs:
      runner:
        description: "Runner label to use (for example, ubuntu-latest or a self-hosted runner label)"
        required: false
        type: string
        default: ${{ vars.RUNNER_LINUX }}
      nodeVersion:
        description: "Node.js version (passed to actions/setup-node)"
        required: false
        type: string
        default: "20"
      packageScope:
        description: "npm scope used to authenticate against GitHub Packages (e.g., '@nfe')"
        required: false
        type: string
        default: "@nfe"
      skipTests:
        description: "Skip the test step (useful for projects without tests or with infra dependencies)"
        required: false
        type: boolean
        default: false
      skipLint:
        description: "Skip the lint step"
        required: false
        type: boolean
        default: false
      skipBuild:
        description: "Skip the build step (useful for addons or workspaces that aren't independently buildable)"
        required: false
        type: boolean
        default: false
      installCommand:
        description: "Command used to install dependencies. Use 'npm install' for repos without a lockfile; 'npm ci' otherwise."
        required: false
        type: string
        default: "npm ci"
      cacheDependencies:
        description: "Enable actions/setup-node npm cache. Requires a lockfile. Set to false when the repo has no lockfile."
        required: false
        type: boolean
        default: true
      buildCommand:
        description: "npm script used to build the project"
        required: false
        type: string
        default: "npm run build"
      lintCommand:
        description: "npm script used to lint the project"
        required: false
        type: string
        default: "npm run lint"
      testCommand:
        description: "npm script used to test the project"
        required: false
        type: string
        default: "npm test"
      uploadArtifact:
        description: "Upload the build output directory as a workflow artifact"
        required: false
        type: boolean
        default: false
      buildOutputDir:
        description: "Build output directory uploaded when uploadArtifact is true"
        required: false
        type: string
        default: "dist"

jobs:
  validate:
    runs-on: ${{ inputs.runner }}
    timeout-minutes: 20
    permissions:
      contents: read
      packages: read

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js (with cache)
        if: ${{ inputs.cacheDependencies }}
        uses: actions/setup-node@v4
        with:
          node-version: ${{ inputs.nodeVersion }}
          registry-url: https://npm.pkg.github.com
          scope: ${{ inputs.packageScope }}
          cache: npm

      - name: Setup Node.js (no cache)
        if: ${{ !inputs.cacheDependencies }}
        uses: actions/setup-node@v4
        with:
          node-version: ${{ inputs.nodeVersion }}
          registry-url: https://npm.pkg.github.com
          scope: ${{ inputs.packageScope }}

      - name: Install dependencies
        env:
          NODE_AUTH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: ${{ inputs.installCommand }}

      - name: Lint
        if: ${{ !inputs.skipLint }}
        run: ${{ inputs.lintCommand }}

      - name: Test
        if: ${{ !inputs.skipTests }}
        run: ${{ inputs.testCommand }}

      - name: Build
        if: ${{ !inputs.skipBuild }}
        run: ${{ inputs.buildCommand }}

      - name: Upload build artifact
        if: ${{ inputs.uploadArtifact }}
        uses: actions/upload-artifact@v4
        with:
          name: build-output
          path: ${{ inputs.buildOutputDir }}
          retention-days: 7
