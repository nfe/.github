name: Deploy container via ArgoCD

on:
  workflow_call:
    inputs:
      appName:
        description: "ArgoCD application name (e.g., xml2pdf-api). Also used as prefix for the GitHub environment '{appName}-{stage}'."
        required: true
        type: string
      stage:
        description: "Deployment stage suffix (prod, staging, homolog). Composed with appName into the GitHub environment '{appName}-{stage}'."
        required: false
        type: string
        default: "prod"
      namespace:
        description: "Target Kubernetes namespace"
        required: true
        type: string
      image:
        description: "Image repository without tag (e.g., ghcr.io/nfe/dfetech-xml2pdf-api)"
        required: true
        type: string
      version:
        description: "Image tag to deploy (SemVer, e.g., 1.0.0)"
        required: true
        type: string
      chartPath:
        description: "Path to the Helm chart within the calling repo"
        required: false
        type: string
        default: "kubernetes"
      valuesFile:
        description: "Values file name within chartPath"
        required: false
        type: string
        default: "values-production.yaml"
      revision:
        description: "Git revision (sha, branch, or tag) for ArgoCD to sync. Defaults to github.sha."
        required: false
        type: string
        default: ""
      extraHelmSet:
        description: "Additional `--helm-set` arguments, one `KEY=VALUE` per line. Appended after the default image.repository/image.tag helm-set."
        required: false
        type: string
        default: ""
    secrets:
      ARGOCD_AUTH_TOKEN:
        required: true

concurrency:
  group: argocd-${{ github.repository }}-${{ inputs.appName }}-${{ inputs.stage }}
  cancel-in-progress: false

jobs:
  deploy:
    runs-on: ${{ fromJSON(vars.RUNNER_LINUX) }}
    timeout-minutes: 15
    environment: ${{ inputs.appName }}-${{ inputs.stage }}
    permissions:
      contents: read
    steps:
      - name: Install ArgoCD CLI
        run: |
          curl -sSL -o /tmp/argocd https://github.com/argoproj/argo-cd/releases/latest/download/argocd-linux-amd64
          sudo install -m 555 /tmp/argocd /usr/local/bin/argocd
          argocd version --client

      - name: Upsert ArgoCD application
        shell: bash
        env:
          REVISION: ${{ inputs.revision != '' && inputs.revision || github.sha }}
        run: |
          EXTRA_HELM_SET=$(cat <<'HELM_SET_EOF'
          ${{ inputs.extraHelmSet }}
          HELM_SET_EOF
          )

          extra_args=()
          while IFS= read -r line; do
            [ -z "$line" ] && continue
            if [[ ! "$line" =~ ^[^=]+=.+ ]]; then
              echo "::error::extraHelmSet line does not match 'KEY=VALUE': $line"
              exit 1
            fi
            extra_args+=(--helm-set "$line")
          done <<< "$EXTRA_HELM_SET"

          argocd app create "${{ inputs.appName }}" \
            --auth-token "${{ secrets.ARGOCD_AUTH_TOKEN }}" \
            --server "${{ vars.ARGOCD_SERVER_URL }}" \
            --app-namespace "${{ vars.ARGOCD_APP_NAMESPACE }}" \
            --project "${{ vars.ARGOCD_PROJECT }}" \
            --dest-server "${{ vars.ARGOCD_DESTINATION_CLUSTER }}" \
            --dest-namespace "${{ inputs.namespace }}" \
            --repo "${{ github.server_url }}/${{ github.repository }}.git" \
            --path "${{ inputs.chartPath }}" \
            --values "${{ inputs.valuesFile }}" \
            --revision "${REVISION}" \
            --helm-set image.repository=${{ inputs.image }} \
            --helm-set image.tag=${{ inputs.version }} \
            "${extra_args[@]}" \
            --sync-policy automated \
            --sync-option Prune=true \
            --sync-option SelfHeal=true \
            --sync-option CreateNamespace=true \
            --upsert \
            --grpc-web

      - name: Wait for sync and health
        run: |
          argocd app wait "${{ inputs.appName }}" \
            --auth-token "${{ secrets.ARGOCD_AUTH_TOKEN }}" \
            --server "${{ vars.ARGOCD_SERVER_URL }}" \
            --app-namespace "${{ vars.ARGOCD_APP_NAMESPACE }}" \
            --health \
            --sync \
            --timeout 600 \
            --grpc-web

  rollback:
    needs: deploy
    if: |
      always()
      && needs.deploy.result == 'failure'
    runs-on: ${{ fromJSON(vars.RUNNER_LINUX) }}
    timeout-minutes: 15
    environment: rollback-approval
    permissions:
      contents: read
    steps:
      - name: Install ArgoCD CLI
        run: |
          curl -sSL -o /tmp/argocd https://github.com/argoproj/argo-cd/releases/latest/download/argocd-linux-amd64
          sudo install -m 555 /tmp/argocd /usr/local/bin/argocd

      - name: Rollback to previous deployment
        id: revert
        continue-on-error: true
        run: |
          PREV_ID=$(argocd app history "${{ inputs.appName }}" \
            --auth-token "${{ secrets.ARGOCD_AUTH_TOKEN }}" \
            --server "${{ vars.ARGOCD_SERVER_URL }}" \
            --app-namespace "${{ vars.ARGOCD_APP_NAMESPACE }}" \
            --grpc-web -o json | jq '.[1].id // empty')

          if [ -z "$PREV_ID" ]; then
            echo "::warning::No previous history entry — first deploy, nothing to rollback to"
            exit 1
          fi

          echo "Rolling back to history entry $PREV_ID (auto-sync will be disabled)"
          argocd app rollback "${{ inputs.appName }}" "$PREV_ID" \
            --auth-token "${{ secrets.ARGOCD_AUTH_TOKEN }}" \
            --server "${{ vars.ARGOCD_SERVER_URL }}" \
            --app-namespace "${{ vars.ARGOCD_APP_NAMESPACE }}" \
            --grpc-web

      - name: Wait for rollback health
        if: steps.revert.outcome == 'success'
        continue-on-error: true
        run: |
          argocd app wait "${{ inputs.appName }}" \
            --auth-token "${{ secrets.ARGOCD_AUTH_TOKEN }}" \
            --server "${{ vars.ARGOCD_SERVER_URL }}" \
            --app-namespace "${{ vars.ARGOCD_APP_NAMESPACE }}" \
            --health \
            --timeout 600 \
            --grpc-web
