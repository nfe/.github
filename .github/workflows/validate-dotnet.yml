name: Validate .NET solution

on:
  workflow_call:
    inputs:
      solution:
        description: "Path to the .sln (or .csproj) file to build and test"
        required: true
        type: string
      runner:
        description: "Runner label to use (for example, ubuntu-latest, windows-latest, or a self-hosted runner label)"
        required: false
        type: string
        default: "ubuntu-latest"
      skipTests:
        description: "Skip the dotnet test step (useful for projects without tests or with Windows-only / SF dependencies)"
        required: false
        type: boolean
        default: false

jobs:
  validate:
    runs-on: ${{ inputs.runner }}
    timeout-minutes: 15
    permissions:
      contents: read
      packages: read

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup .NET SDK
        uses: actions/setup-dotnet@v4
        with:
          global-json-file: global.json

      - name: Setup NuGet CLI
        uses: NuGet/setup-nuget@v2

      # WORKAROUND: we use `nuget restore` (legacy CLI) instead of `dotnet restore`
      # because it's the only tool that handles both PackageReference and
      # packages.config in one pass — the latter is still needed by Service
      # Fabric sfproj's SDK package (`Microsoft.VisualStudio.Azure.Fabric.MSBuild`).
      # Switch back to `dotnet restore` once Service Fabric is no longer in use
      # anywhere in the org. See nfe/ai-governance#3 for the broader migration.
      - name: Restore
        env:
          GITHUB_USERNAME: ${{ github.actor }}
          GITHUB_PACKAGES_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: nuget restore "${{ inputs.solution }}"

      - name: Build
        run: dotnet build "${{ inputs.solution }}" --no-restore -c Release

      - name: Test
        if: ${{ !inputs.skipTests }}
        run: dotnet test "${{ inputs.solution }}" --no-build -c Release --logger "console;verbosity=minimal"
